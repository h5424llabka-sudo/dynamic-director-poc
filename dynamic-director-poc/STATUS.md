# Dynamic Director AI PoC - 実装ステータス

現在の実装状況を整理したドキュメントです。

## 全体進捗 (フェーズ 1〜4 完了)

- [x] **フェーズ 1:** プロジェクトのセットアップとCI/CD
- [x] **フェーズ 2:** ローカル検証スクリプトとモデル準備
- [x] **フェーズ 3:** Android カメラUIとフレーム取得基盤
- [x] **フェーズ 4:** コア推論・スコアリングロジックの実装
- [ ] **フェーズ 5:** 自動撮影（シャッター）の統合

## 現在アプリに実装済みの機能

1. **カメラプレビューと権限管理**
   - 初回起動時のカメラ権限リクエスト機能。
   - CameraXとJetpack Composeを利用したフルスクリーンのカメラ映像のリアルタイム表示。
2. **AI人物検出 (YOLOv8 + TFLite)**
   - `YoloDetector.kt` にて、TensorFlow Liteを用いた軽量エッジ推論を実行。
   - カメラフレームからリアルタイムで人物（Person）のバウンディングボックス（BBox）を抽出。
3. **構図・光量スコアリング (OpenCV)**
   - `ScoringEngine.kt` にて、`config.json` に定義された理想の設定値（被写体の位置・サイズ・輝度）と現在のカメラ映像を比較し、一致度スコア（0〜100%）を算出。
4. **UIダッシュボード表示**
   - 画面左上に現在のスコア、AIの検出ステータス（Confidence等）、および**アプリのバージョン番号**をリアルタイム表示。

## 未実装の機能 (次のステップ: フェーズ5)

今後は、現在リアルタイムで算出されているスコアを活用し、**「スコアが一定の閾値（例: 85%）を3フレーム連続で超えた瞬間に、自動でシャッターを切り（端末に高画質写真を保存し）、その後3秒間連続撮影をストップする（クールダウン）」**という自動撮影ロジックを実装する予定です。
