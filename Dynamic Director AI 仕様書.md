ご要望を踏まえ、アプリの概要、目的、そしてユーザー体験（UX）からシステム内部の処理に至る詳細なフローを追加し、網羅的なPoC開発仕様書として再構築しました。

# **「Dynamic Director AI」Android Native PoC 開発仕様書**

## **1\. アプリの概要**

本アプリケーションは、スマートフォンのカメラ映像をエッジAIがリアルタイムで解析し、あらかじめ設定した「理想の構図・光条件」と完全に一致した瞬間に、自動でシャッターを切るインテリジェントな撮影アシストアプリ（Co-photographer）です。ユーザーが指定した1枚のリファレンス画像（お手本となる写真）から、VLM（Vision-Language Model）が空間的・光学的条件をパラメータとして抽出し、それをエッジデバイス上の推論エンジンが連続監視することで、「条件が揃ったエモい瞬間の自動記録」を実現します。

## **2\. アプリの目的**

* **撮影者の認知的負荷の排除:** 常に動き回る被写体（子供など）に対して、スマートフォンの画面を凝視してシャッターチャンスを伺う煩わしさを解消します。  
* **コミュニケーションと記録の高度な両立:** カメラを固定して放置するだけでシステムが自動撮影を行うため、親は撮影を忘れ、息子と一緒に全力で遊ぶことや直接的なコミュニケーションに集中できます。  
* **プロ品質の再現の自動化:** 普段こだわって撮影・現像しているような理想的なトーンやアングル（三分割構図や逆光の輝度比など）をシステムに学習させることで、技術的・時間的なリソースが枯渇している場面でも高品質な記録を担保します。

## **3\. アプリのフロー**

### **ユーザー体験（UX）フロー**

> 1. **事前準備:** ユーザーは「撮りたい雰囲気（リファレンス画像）」をアプリに登録します。  
> 2. **パラメータ生成:** クラウドのVLMが画像を解析し、構図と光の条件を抽出して設定ファイル（JSON）としてアプリ内に保存します。  
> 3. **撮影セットアップ:** ユーザーはアプリの撮影画面を起動し、公園や庭のベンチなどにスマートフォンを立てかける、あるいはざっくりと構えます。  
> 4. **ハンズフリー撮影:** ユーザーはスマートフォンの画面を見ず、被写体との時間（遊び）に没入します。  
> 5. **自動記録:** 被写体が理想の構図・光の空間に入った瞬間、アプリがサイレントで高解像度写真を自動保存します。

### **システム処理フロー（リアルタイム推論ループ）**

> 1. **フレーム取得:** CameraXがカメラから最新フレーム（YUV形式）を取得し、推論用のRGBビットマップに変換します。  
> 2. **被写体検出:** TFLiteモデル（YOLO）がフレームを解析し、人物のバウンディングボックス（BBox）座標を取得します。  
> 3. **光量解析:** OpenCVがBBoxを元の解像度にマッピングし、HSV色空間にて被写体と背景の明度（V値）の中央値を算出します。  
> 4. **スコアリング:** 事前設定（JSON）の理想値と現在のBBox位置・輝度比の誤差を計算し、一致度スコア（0〜100%）を算出します。  
> 5. **シャッター判定:** スコアが閾値（例：85%）を3フレーム連続で超えた場合、ImageCapture APIで写真を保存します。  
> 6. **クールダウン:** 連続撮影を防ぐため、保存後3秒間は推論処理をスキップし、その後ループに復帰します。

## **4\. 開発・デプロイメントワークフロー**

GitHubをソースコードとモデルの管理ハブとし、CI/CDでAndroidネイティブアプリを自動ビルドする構成とします。

> 1. **ローカル開発:** PythonでYOLOモデルのTFLite変換とスコアリングロジックを検証し、Android StudioでKotlin \+ CameraXのUIを実装します。  
> 2. **GitHubへPush:** ソースコード、軽量化モデル、リファレンス用 config.json をリポジトリにPushします。  
> 3. **自動ビルド (GitHub Actions):** PushをトリガーにGradleビルドを実行し、APKアーティファクトを自動生成します。  
> 4. **実機デプロイ:** 生成されたAPKをAndroid端末にインストールし、フィールドテストを実施します。

## **5\. システムアーキテクチャと技術スタック**

| コンポーネント | 採用技術 | 役割 |
| :---- | :---- | :---- |
| **フロントエンド** | Kotlin, Jetpack Compose | UI描画、権限管理、開発者用ダッシュボードの表示 |
| **カメラ制御** | CameraX API | プレビュー表示、推論用フレーム抽出（ImageAnalysis）、シャッター実行（ImageCapture） |
| **推論エンジン** | TensorFlow Lite | YOLOv8n（または11n）を用いたモバイル環境での高速な被写体検出 |
| **画像処理** | OpenCV for Android | BBox内外のHSV変換、輝度計算、設定値との誤差演算 |
| **事前解析API** | Gemini 1.5 Flash API | リファレンス画像からのパラメータ抽出（JSON生成） |
| **CI/CD** | GitHub Actions | Ubuntu環境でのJDKセットアップ、APK自動ビルド |

## **6\. ディレクトリ構成（GitHubリポジトリ）**

Plaintext  
dynamic-director-poc/  
├── .github/  
│   └── workflows/  
│       └── android-build.yml     \# APK自動ビルド用のCI設定ファイル  
├── app/                          \# Androidプロジェクトルート  
│   ├── src/main/  
│   │   ├── java/com/example/director/ \# Kotlinソースコード  
│   │   ├── cpp/                       \# OpenCV高速処理用JNIコード  
│   │   └── assets/  
│   │       ├── yolov8n\_float16.tflite \# 変換済みの軽量検出モデル  
│   │       └── config.json            \# VLMから抽出した構図・光パラメータ  
│   └── build.gradle.kts  
├── scripts/                      \# ローカル検証用Pythonスクリプト  
│   ├── export\_model.py           \# PyTorch(YOLO) \-\> TFLite変換用  
│   └── simulate\_scoring.py       \# PCウェブカメラでのロジック検証用  
└── README.md

## **7\. データ構造（構図パラメータ JSON）**

VLMから出力させ、推論エンジンに渡す構成ファイル（assets/config.json）の仕様です。

JSON  
{  
  "target\_composition": {  
    "subject\_center\_x": 0.66,  
    "subject\_center\_y": 0.50,  
    "subject\_area\_ratio": 0.25  
  },  
  "target\_lighting": {  
    "is\_backlit": true,  
    "subject\_brightness\_target": 120,  
    "background\_brightness\_target": 200  
  },  
  "tolerance": {  
    "position\_margin": 0.1,  
    "area\_margin": 0.05  
  }  
}

概要と目的が明確になったことで、技術的な要件だけでなく「誰のどんな課題を解決するためのコードか」がブレない、非常に完成度の高い仕様書になったかと思います。次に進めるにあたり、Python側でのモデルエクスポートスクリプトの作成に着手しますか？