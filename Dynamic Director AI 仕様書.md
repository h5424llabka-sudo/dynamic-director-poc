# 「Dynamic Director AI」 統合開発仕様書 (v0.7.0)

## 1. アプリの概要

本アプリケーションは、スマートフォンのカメラ映像をオンデバイス（エッジ）AIがリアルタイムで解析し、特に動きの予測が難しく撮影難易度が高い **「2〜3歳児のエモーショナルな瞬間」** を自律的に判断して自動撮影を行うインテリジェントな撮影アシストアプリ（Co-photographer）です。

単なる自動撮影にとどまらず、時系列骨格推定エンジン、ヒステリシス＆確定ウィンドウによる誤検知防止トリガー制御、プロのような構図再現（リファレンス撮影）、セグメンテーションによるアーティスティックな編集、そして位置情報や3D空間を活用した閲覧体験までを包括する次世代のAIカメラプラットフォームを目指します。

---

## 2. コアバリューと最終ビジョン

1. **決定的瞬間を逃さない自律撮影 (Intelligent Auto-Shutter)**
   - 常に動き回る幼児に対し、「カメラ目線＋笑顔」や「全身を使った感情表現（万歳、指さし、バイバイ等）」を瞬時に捉え、人間の反射神経では間に合わない決定的瞬間を自動記録。
2. **誤検知・連写破綻を防ぐスマートトリガー制御 (Hysteresis & Confirmation)**
   - 瞬間的なノイズや類似姿勢による誤動作を排除し、撮影後の適切なクールダウン管理によって写真フォルダの乱造を防止。
3. **アーティスティックな編集機能 (AI Segmentation & Compositing - ※次期フェーズ)**
   - 人物と背景を高精度に分離し、一眼レフ風の自然な被写界深度（ボケ効果）や別背景との合成を実現。
4. **プロ水準のリファレンス撮影アシスタント (Reference-based Directing - ※次期フェーズ)**
   - VLM（Vision-Language Model）がお手本写真の構図・光・画角を解析し、画角が揃った瞬間に自動撮影。
5. **独創的な写真の記録・閲覧体験 (Creative Gallery - ※次期フェーズ)**
   - 位置情報Exifと連動したマップビューやバーチャル3D展示空間でのエモーショナルな振り返りを提供。

---

## 3. AI認識パイプラインと自動撮影トリガー

撮影トリガーは、「表情・目線（Face）」と「全身の動作（Pose）」の2系統の並行パイプラインで構成されています。

```text
[CameraX プレビューフレーム]
       │
       ├───> [FaceAnalyzer] ──> 顔角度(Yaw/Pitch) & 笑顔確率 ──> [Smile Trigger]
       │
       └───> [PoseAnalyzer] (ML Kit Pose Detection)
                 │ (2D関節座標 33点)
                 ▼
             [PoseTimeSeriesBuffer] (リングバッファ)
                 ・30Hz等時間リサンプリング (FPS非依存化)
                 ・肩幅・腰基準の空間正規化 (体格・カメラ距離の吸収)
                 ・欠損フレームの線形補間
                 │ (正規化特徴量: 9主要関節 × 3ch = 27次元 × 30フレーム)
                 ▼
             [ActionClassifier] (時系列アクション評価)
                 ・幾何学的特徴量 (肘・肩の屈曲角度)
                 ・動的特徴量 (速度・角速度・周波数)
                 ・(将来) Depthwise Separable 1D-CNN (TFLite)
                 │ (アクション名 & 信頼度スコア 0.0〜1.0)
                 ▼
             [TriggerController] (シャッター発火判定)
                 ▲ (構図マッチングスコア)
                 │
             [ReferenceDirector] (お手本構図アシスト)
                 ・Gemini VLM によって生成された構図ルール(モック)を保持
                 ・Face/Pose座標からリアルタイムに一致度(0.0〜1.0)を算出
                 ・個別アショントグル検証
                 ・Activation閾値 & Deactivation閾値 (ヒステリシス)
                 ・Confirmation Window (連続Nフレーム確定)
                 ・Cooldownタイマー管理
                 │
                 ▼
             [Best Shot Buffer] (自動笑顔抽出)
                 ・トリガー発火前後の数フレーム（最大20フレーム）をリングバッファで保持
                 ・トリガー後、未来フレームを規定数収集
                 ・バッファ内から最高 `smileProbability` のフレームを選出
                 │
                 ▼
          【自動シャッター実行 (ImageCapture)】
```

### 3.1 表情・目線トリガー (Face Trigger)
- **カメラ目線判定**: 頭部の水平回転（Yaw / Euler Y）および垂直傾き（Pitch / Euler X）が正面（±15度以内）であること。
- **笑顔判定**: 笑顔確率（Smiling Probability）が設定された閾値（デフォルト80%）を超えていること。

### 3.2 アクション・動作トリガー (Pose Action Trigger)
2〜3歳児特有の頭身比（頭が大きく手足が短い）や不規則な動きに対応するため、空間正規化された時系列データから以下のアクションを判定します。

| アクション名 | 判定アルゴリズム・特徴量 | 捉えるエモーショナルなシーン |
| :--- | :--- | :--- |
| **万歳 (Banzai)** | 両手首が肩より高く、かつ両肘の角度が135度以上（腕がまっすぐ上に伸びている）。Y軸変位が正。 | 歓喜の瞬間、お着替え成功、達成感 |
| **指さし (Pointing)** | いずれか一方の手首が肩幅の1.2倍以上前・横に伸展し、かつ肘角度が140度以上維持。もう一方の手は伸展していない。 | 発見の瞬間、「あれ見て！」、興味関心 |
| **手を振る (Waving)** | 手首が肩より高い位置にあり、過去0.5〜1秒間に手首X座標の反復振動（周波数1.5〜5Hzのゼロ交差）を検出。 | 「バイバイ」、お見送り、呼びかけ |
| **投げる (Throwing)** | 手首が頭部上方から前方下方向へ急激に加速（閾値以上の下向き速度・角速度ピークを検出）。 | ボール投げ、水しぶき遊び、ダイナミックな動作 |
| **手をたたく (Clapping)** | 両手首の距離が胸元（肩と腰の間）で周期的に接近・離反（0.3秒以内に距離が急縮小し、かつ振動）。 | 拍手、「上手上手」、音楽に合わせたリズム遊び |

### 3.3 ベストショットバッファ (Best Shot Buffer)
トリガーが発火した瞬間の画像をそのまま保存するのではなく、「一番良い表情」を自動で選び出す機能です。
- **リングバッファ保持**: 常に直近20フレーム（約0.6秒分）の画像と笑顔スコアをメモリ上に一時保持します。
- **未来フレームの収集**: トリガー発火後、即座に保存せず、さらに未来10フレーム（約0.3秒）を収集します。
- **最高笑顔の抽出**: 過去〜未来の計20フレームの中から、最も笑顔確率（`smileProbability`）が高いフレームを選定し、最終的な写真として保存します。これにより、シャッターの僅かな遅れや瞬きなどを自動的に補正します。
- **アーティスティックな編集 (SegmentationEngine)**: 保存される写真に対してのみ、ML Kit Subject Segmentation を用いて前景マスクを抽出し、OpenCVを用いたガウシアンブラーで背景をぼかす「被写体強調処理」を施します。

### 3.4 リファレンス構図アシスト (Reference Directing)
指定した「お手本写真」の構図に近づいた瞬間に自動撮影する機能です。
- **ハイブリッド・エッジアーキテクチャ**: リアルタイムのカメラフレームをすべてクラウド（Gemini API等）に送るのではなく、事前にお手本写真から「構図ルール（例: 顔が画面中央、特定のポーズ）」をVLMで抽出しローカルに保持します。
- **リアルタイムマッチング**: `ReferenceDirector` が、ローカルの `FaceAnalyzer` および `PoseAnalyzer` から得られる座標情報と構図ルールをリアルタイムに比較し、マッチングスコア（0.0〜1.0）を `TriggerController` へ継続的に送信します。これにより遅延のない構図アシスト撮影を実現します。

---

## 4. 【詳細解説】設定項目（Settings & Parameters）

本アプリでは、撮影環境や子供の性格・月齢、撮影意図に合わせて振る舞いを細かくカスタマイズできる設定UIを提供しています。

### 4.1 設定パラメータ一覧表

| 設定項目 | UI表示名 | 調整可能範囲 | デフォルト値 | 影響する制御ロジック |
| :--- | :--- | :--- | :--- | :--- |
| **笑顔閾値** | `Smile Threshold` | 0% 〜 100% (5%刻み) | **80%** | `FaceAnalyzer` の笑顔判定 |
| **撮影クールダウン** | `Cooldown Duration` | 1秒 〜 10秒 (1秒刻み) | **3秒** | `TriggerController` の撮影インターバル |
| **アクション発火閾値** | `Activation Threshold` | 30% 〜 100% (5%刻み) | **70%** | `TriggerController` のヒステリシス上限値 |
| **解除閾値 (内部)** | `Deactivation Threshold` | 固定 (発火閾値の約60%) | **40%** | `TriggerController` のヒステリシス下限値 |
| **確定フレーム数** | `Confirmation Frames` | 1 〜 10フレーム | **3フレーム** | `TriggerController` の確定ウィンドウ |
| **アクション別トグル** | `Banzai / Pointing / ...` | ON / OFF スイッチ | **すべてON** | `TriggerController` の対象アクションフィルタ |

---

### 4.2 各設定項目の目的・挙動とチューニング指針

#### ① 笑顔閾値 (`Smile Threshold`)
* **パラメータ概要**: ML Kit Face Detectionが算出する「笑顔確率 (0.0〜1.0)」に対する足切り基準値です。
* **挙動への影響**:
  * **高く設定 (85%〜100%)**: 「満面の笑み」「大爆笑」の瞬間のみに絞り込まれます。日常の軽い微笑みではシャッターが切られなくなり、枚数は減りますがベストショット率が極めて高くなります。
  * **低く設定 (50%〜70%)**: 少し口角が上がった程度の自然な表情や、話している最中の和やかな表情でもシャッターが切れます。シャッターチャンスは増えますが、半笑いや口を開けただけの顔が混ざる可能性があります。
* **推奨設定**: 通常は **80%**。屋外でよく笑う活発なシーンでは **85%**、室内で落ち着いて遊んでいる時は **70%** が適しています。

#### ② 撮影クールダウン (`Cooldown Duration`)
* **パラメータ概要**: 一度自動撮影が実行されてから、次の自動撮影を許可するまでの待機時間（秒）です。
* **挙動への影響**:
  * **短く設定 (1〜2秒)**: 連続したアクション（例: 万歳をしながら喜び続けている最中や、連続して手を振っている間）に複数のカットを記録できます。ただし、ストレージ消費が激しくなり、似たような構図の写真が大量に保存されます。
  * **長く設定 (5〜10秒)**: 1つのアクションにつき1枚だけを厳選して残します。被写体が次の別の遊びに移るまで待機するため、写真フォルダが重複写真で圧迫されるのを防ぎます。
* **推奨設定**: 普段使いでは **3秒**。決定的瞬間を逃したくない重要イベント（運動会、誕生日ケーキのロウソク消し等）では **1〜2秒**。

#### ③ アクション発火閾値 (`Activation Threshold`)
* **パラメータ概要**: `ActionClassifier` が判定したアクションの信頼度スコア（0.0〜1.0）がこの値を超えた時、トリガーの「発火候補」状態へ遷移します。
* **ヒステリシス（Hysteresis）制御の仕組み**:
  * 単一の閾値でON/OFFを切り替えると、スコアが境界線上（例: 69%と71%を行き来する状態）で細かく振動した際に「発火・解除」が高速でチャタリングし、誤作動の原因になります。
  * そこで、**発火（Activation）には高めの閾値（例: 70%）** を要求し、一度検知状態に入った後は **解除（Deactivation）閾値（40%）を下回るまで検知状態を維持** する「ヒステリシス構造」を採用しています。
* **挙動への影響**:
  * **高く設定 (80%〜90%)**: 完全なフォーム（しっかり腕が伸びた万歳、明確な指さし）のみを厳密に検出します。誤検知はほぼゼロになりますが、子供の崩れたポーズを取りこぼすことがあります。
  * **低く設定 (50%〜60%)**: 少し肘が曲がっている、または斜め向きのポーズでも敏感に反応します。チャンスは広がりますが、単に手を挙げただけの動作を誤検知しやすくなります。
* **推奨設定**: バランス型の **70%**。

#### ④ 確定フレーム数 (`Confirmation Frames`)
* **パラメータ概要**: 発火閾値を超えた状態が、**「何フレーム連続で継続したか」** をカウントし、指定フレーム数に達して初めて本物のシャッターを切る「確定ウィンドウ（Confirmation Window）」の長さです。
* **挙動への影響**:
  * **小さく設定 (1フレーム)**: アクションスコアが閾値を超えた瞬間に即座にシャッターが切れます。俊敏な動き（一瞬のパンチや素早い投げ動作）に強いですが、ML Kitの骨格推定が1フレームだけ誤認識したノイズでもシャッターが切れてしまいます。
  * **大きく設定 (5〜10フレーム)**: 1秒間に約30fps処理されている場合、5フレームは約0.17秒のポーズ維持が必要です。ノイズ誤検知は完全に排除されますが、一瞬で終わるポーズの場合、シャッターが切れる前にポーズが解けてしまう「シャッター遅れ」が発生します。
* **推奨設定**:
  * 万歳・指さし・拍手など静止ポーズを含む動作: **3フレーム (約0.1秒)**
  * 激しく動くシーン: **2フレーム**

#### ⑤ 個別アクショントグル (`Banzai / Pointing / Waving / Throwing / Clapping`)
* **パラメータ概要**: 各アクションの自動撮影を有効にするかどうかの個別スイッチです。
* **ユースケース**:
  * 「今日は公園でボール投げの練習をするから **Throwing** だけを狙いたい」
  * 「普段からよく手を挙げる癖があり万歳が誤爆しやすいので、**Banzai** のみOFFにする」
  * 狙いたいシャッターチャンスに応じて絞り込むことで、意図しない撮影を大幅に削減できます。

---

### 4.3 シーン別おすすめプリセット設定

| プリセット名 | 笑顔閾値 | クールダウン | 発火閾値 | 確定フレーム | 推奨設定の狙い |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **公園・アクティブ** | 75% | 2秒 | 65% | 2フレーム | すばやい動きを取りこぼさず、ダイナミックな瞬間を連写気味に捉える |
| **日常・おうちスナップ** | 80% | 3秒 | 70% | 3フレーム | 誤検知とシャッターチャンスのバランスが最も良い標準設定 |
| **笑顔重視ポートレート** | 85% | 4秒 | 80% | 4フレーム | 表情とポーズのクオリティを最優先。確実に決まった写真だけを残す |
| **赤ちゃん・1歳児向け** | 70% | 3秒 | 60% | 2フレーム | 身体が小さくポーズが曖昧な月齢向けに、閾値を緩めてチャンスを最大化 |

---

## 5. システムアーキテクチャと技術スタック

| コンポーネント | 採用技術 | 役割と詳細仕様 |
| :--- | :--- | :--- |
| **UI / フロントエンド** | Kotlin, Jetpack Compose | リアルタイムプレビュー描画、設定ボトムシート、リアルタイムデバッグダッシュボード |
| **カメラ制御** | CameraX (1.3+) | `ImageAnalysis` (30fpsストリーム解析) と `ImageCapture` (高画質JPEG撮影) の並行運用 |
| **顔・表情検出** | Google ML Kit Face Detection | 顔向き角度（Yaw, Pitch, Roll）、笑顔スコアのリアルタイム算出 |
| **骨格時系列バッファ** | `PoseTimeSeriesBuffer` | 30フレームリングバッファ、等時間リサンプリング、肩幅スケーリング正規化 |
| **アクション分類器** | `ActionClassifier` | 幾何学的・動的特徴量によるルールベース分類（+ 1D-CNN TFLite連携対応） |
| **お手本構図評価** | `ReferenceDirector` | Gemini VLMの構図ルールに基づく、リアルタイムのFace/Pose座標マッチング |
| **トリガー管理** | `TriggerController` | ヒステリシス制御、確定ウィンドウ判定、クールダウンタイマーのステートマシン |
| **アーティスティック編集** | `SegmentationEngine` | ML Kit Subject Segmentation & OpenCV を用いたベストショットの背景ボケ処理 |
| **MLモデル学習環境** | Python, PyTorch, ONNX, TFLite | 幼児動作の合成時系列データ生成（`generate_synthetic_data.py`）および1D-CNN学習（`train_action_model.py`） |
| **画像メタデータ・保存** | Android MediaStore API | 撮影画像の保存、デバッグ用トリガー理由ウォーターマークの書き込み |

---

## 6. ディレクトリ構成

```text
dynamic-director-ai/
├── Dynamic Director AI 仕様書.md            # 本仕様書
├── dynamic-director-poc/                     # Android アプリケーションプロジェクト
│   ├── app/src/main/
│   │   ├── java/com/example/director/
│   │   │   ├── MainActivity.kt               # メイン画面、CameraXライフサイクル、設定UI、ダッシュボード
│   │   │   ├── FaceAnalyzer.kt               # 顔・笑顔検出エンジン
│   │   │   ├── PoseAnalyzer.kt               # 骨格検出およびパイプライン統合
│   │   │   ├── PoseTimeSeriesBuffer.kt       # 【新規】時系列リングバッファ & 空間正規化
│   │   │   ├── ActionClassifier.kt           # 幼児向け高精度アクション認識エンジン
│   │   │   ├── TriggerController.kt          # ヒステリシス・確定・クールダウン制御
│   │   │   ├── SegmentationEngine.kt         # 【新規】背景ボケ(Bokeh)効果・セグメンテーション
│   │   │   ├── ReferenceDirector.kt          # 【新規】構図ルールマッチングエンジン
│   │   │   └── OverlayView.kt                # 骨格ランドマーク描画
│   │   └── assets/                           # TFLiteモデル配置用ディレクトリ
│   └── build.gradle.kts
└── scripts/                                  # MLモデル開発スクリプト
    ├── generate_synthetic_data.py            # 幼児アクション合成時系列データ生成
    ├── train_action_model.py                 # Depthwise 1D-CNN学習 & TFLiteエクスポート
    ├── data/                                 # 学習用合成データセット (.npz)
    └── models/                               # エクスポート済みTFLiteモデル (FP16 / INT8)
```

---

## 7. 今後の拡張ロードマップ

1. **フェーズ 1 (完了・旧 v0.5.0)**:
   - 時系列正規化バッファ＋ヒステリシス・確定ウィンドウによる骨格アクション判定精度の向上
   - 詳細な設定パラメータUIとリアルタイムデバッグダッシュボードの提供
2. **フェーズ 2 (完了・旧 v0.6.0)**:
   - 学習済み 1D-CNN TFLite モデル（32KB軽量モデル）のアプリ組み込みと、ルールベース判定とのアンサンブル推論
   - ベストショットバッファ（シャッター前後数フレームから最も笑顔スコアの高い瞬間を自動選出する機能）
3. **フェーズ 3 (完了・現行 v0.7.0)**:
   - ML Kit Subject Segmentation による自動切り抜きと背景ボケ効果 (Bokeh) の適用。
   - お手本写真の構図アシスト (Reference Directing) エンジンの組み込み。
4. **フェーズ 4 (プラットフォーム化)**:
   - 位置情報Exifを活用したマップギャラリー、3Dバーチャル展示会ビューアーの実装。

---

## 8. 骨格検出・モーション検知の精度をさらに高めるための次世代アプローチ

現在のアプローチ（時系列正規化＋ハイブリッド推論）からさらに一歩進み、より高度に幼児のエモーショナルな瞬間を捉えるための技術的な拡張案です。

### 1. 実データの収集と継続的学習 (Data Flywheel)
- 現在の1D-CNNモデルは合成データ（Synthetic Data）で事前学習されています。アプリを使用する中で得られる実際の幼児の動画から抽出した骨格時系列データを匿名化して収集・アノテーションし、再学習させることで精度が飛躍的に向上します。
- アクティブラーニングの導入：自信度（Confidence）が中途半端なデータや、ユーザーが「お気に入り」登録した写真を正例としてモデルをファインチューニングします。

### 2. 高度な時系列ディープラーニングモデルの採用
- 単純な 1D-CNN に加え、系列データの長期的な文脈を学習できる **LSTM / GRU** や、近年の動画解析で主流となりつつある **Time-Sformer (Transformer系)** を導入します。これにより、「手を挙げただけの動作」と「一連の喜んでいる動作の一部としての万歳」をより正確に区別できます。

### 3. MediaPipe Holistic 等の高密度モデルへの移行
- 現在の ML Kit Pose Detection は全身の主要な関節のみを取得しますが、指先の細かな形状（Vサイン、指差し時の指の伸び）を捉えるには解像度が不足しています。
- 顔・手・身体のランドマークを同時に取得できる **MediaPipe Holistic** などの精密モデルに移行することで、指差しや手遊びなどの微細な表現をより正確に捉えられます。

### 4. マルチモーダル認識の統合
- **音声認識（Audio AI）**: 骨格だけでなく、マイクからの音声データ（笑い声、歓声、「バイバイ」といった発話）をリアルタイム解析し、モーション検知と掛け合わせることで誤検知をほぼゼロに抑えられます。
- **物体認識（Object Detection）**: 幼児が「おもちゃ」や「ボール」を持っているかどうかを検知し、投げる動作や遊びの文脈を理解します。

### 5. 教師なし異常検知 (Anomaly Detection) による「未知のエモい瞬間」の発掘
- 事前に定義された「万歳」「拍手」などの特定のアクションを待つのではなく、オートエンコーダ（AutoEncoder）等を用いて「普段の歩行や静止とは異なる、急激な動きや珍しい姿勢（異常値）」を検知します。
- これにより、開発者が想定していなかった予測不能な「エモい瞬間（例：急に寝転がる、不思議なポーズを取る等）」を自動的に記録する機能が実現できます。